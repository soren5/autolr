[pytest]
testpaths = tests
markers =
    unit: fast tests with no external artifacts or model training
    smoke: fast end-to-end framework mechanics, usually fake fitness
    integration: small real framework integrations
    slow: real dataset/model/GPU/cluster checks

